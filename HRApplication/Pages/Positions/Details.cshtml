@page
@model HRApplication.Pages.Positions.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>Position</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Position.Location)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Position.Location)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Position.Title)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Position.Title)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Position.Description)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Position.Description)
        </dd>
    </dl>
</div>
<div class="btn-group">
    <a class="btn btn-primary" asp-page ="/Applications/Create" asp-route-id="@Model.Position.ID">Apply</a>
    @if (User.IsInRole("HR")){ 
    <a class="btn btn-primary" asp-page="./Edit" asp-route-id="@Model.Position.ID">Edit</a>
    <a class="btn btn-primary" asp-page="/Applications/PositionApplications" asp-route-id="@Model.Position.ID">Manage Applications</a>
    }
    <a class="btn btn-primary" asp-page="./Index">Back to List</a>
</div>
